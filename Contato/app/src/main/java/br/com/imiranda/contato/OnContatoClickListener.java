package br.com.imiranda.contato;

//interface que vai ser implementada pela activity para tratar os enventos de click em cada céula do recicler view

public interface OnContatoClickListener {
    void onContatoClick(int posicao);
}
